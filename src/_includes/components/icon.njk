{% macro icon(name, className) %}
<svg
  class="icon-component {% if className %}{{ className }}{% endif %}"
  aria-hidden="true"
  focusable="false"
>
  {% include "../icons/" + name + ".svg" %}
</svg>
{% endmacro %}
