<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} | {% endif %}Кляузин и партнеры</title>
  <meta name="description" content="{{ description }}">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@200..700&family=Red+Hat+Display:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@200..700&family=Red+Hat+Display:ital,wght@0,300..900;1,300..900&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ baseUrl }}/styles/main.css">
  {% if page.url != "/" %}
    <link rel="stylesheet" href="{{ baseUrl }}{{ page.url }}style.css">
  {% endif %}
  <link rel="icon" href="{{ baseUrl }}/favicon.ico" type="image/x-icon">
</head>
<body>
  <div class="page-wrapper {% if page.url == "/" %} page-wrapper--home {% endif %}">
    {% if page.url != "/" %}
      {% include "components/navigation.njk" %}
    {% endif %}
  
    <main class="main content-grid">
      {{ content | safe }}
    </main>
  </div>
  <script src="{{ baseUrl }}/scripts/main.js"></script>
</body>
</html> 